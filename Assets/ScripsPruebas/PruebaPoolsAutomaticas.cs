using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PruebaPoolsAutomaticas : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //AssetPoolManager.Instance.RegisterAllInFolder();
        //AssetPoolManager.Instance.SpawnPreviews();

        /*
        // Asegúrate de que este GameObject tiene el componente AdaptiveTileTester
        var tester = this.gameObject.GetComponent<AdaptiveTileTester>();
        if (tester == null)
            tester = this.gameObject.AddComponent<AdaptiveTileTester>();

        // Dale la referencia al poolManager si no la tiene
        if (tester.poolManager == null)
            tester.poolManager = AssetPoolManager.Instance;

        tester.RunTest();
        */
    }
}
